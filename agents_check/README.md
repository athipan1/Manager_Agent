# ระบบ Multi-Agent Trading Orchestration

ชุดคำสั่งสำหรับรันระบบเทรดอัตโนมัติที่ประกอบด้วย 7 Agent ทำงานร่วมกัน

## 🚀 วิธีการรันระบบ

1. **เตรียมตัว:** ตรวจสอบว่าคุณมี Docker และ Docker Compose ติดตั้งอยู่ในเครื่อง
2. **รันคำสั่ง:**
   ```bash
   docker compose up --build
   ```
   คำสั่งนี้จะทำการ build image จาก source code ของทุก agent และเริ่มทำงานตามลำดับความสัมพันธ์

## 📂 รายละเอียดการเชื่อมต่อ (Port Mapping)

| Service | Host Port | Internal Port | คำอธิบาย |
|---------|-----------|---------------|----------|
| **Manager-Agent** | 8000 | 80 | จุดรับงานหลัก (Gateway) |
| **Database-Agent** | 8001 | 8000 | จัดการฐานข้อมูลและการบันทึกคำสั่ง |
| **Fundamental-Agent**| 8002 | 8001 | วิเคราะห์พื้นฐานหุ้น |
| **Technical-Agent** | 8003 | 8002 | วิเคราะห์สัญญาณเทคนิค |
| **Learning-Agent** | 8004 | 8004 | ระบบเรียนรู้และปรับปรุงกลยุทธ์ |
| **Execution-Agent** | 8005 | 8005 | ส่งคำสั่งซื้อขายจริง |
| **Scanner-Agent** | 8006 | 8000 | ค้นหาหุ้นที่น่าสนใจ |
| **Postgres DB** | 5432 | 5432 | ฐานข้อมูลหลัก (Shared) |

## 🧪 การทดสอบระบบ

### 1. ตรวจสอบ Health Check
คุณสามารถตรวจสอบความพร้อมของระบบผ่าน Manager Agent:
```bash
curl http://localhost:8000/health
```

### 2. ทดสอบการวิเคราะห์หุ้น (Single Asset)
```bash
curl -X POST http://localhost:8000/analyze \
     -H "Content-Type: application/json" \
     -d '{"ticker": "AAPL"}'
```

### 3. ทดสอบการวิเคราะห์หลายหุ้น (Portfolio)
```bash
curl -X POST http://localhost:8000/analyze-multi \
     -H "Content-Type: application/json" \
     -d '{"tickers": ["AAPL", "GOOGL", "MSFT"]}'
```

## ⚠️ ข้อควรทราบ
* **API Keys:** ปัจจุบันตั้งค่าเริ่มต้นเป็น `master_key` ในทุกตัวเพื่อความสะดวกในการทดสอบ
* **Persistence:** ข้อมูลฐานข้อมูลจะถูกเก็บไว้ที่ Docker Volume ชื่อ `postgres_data`
* **Network:** Agent ต่างๆ คุยกันผ่าน internal network ของ Docker โดยใช้ชื่อ service (เช่น `http://technical-agent:8002`)
