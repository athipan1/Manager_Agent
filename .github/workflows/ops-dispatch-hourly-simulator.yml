name: Ops Diagnose Hourly Simulator

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ops-dispatch-hourly-simulator.yml

permissions:
  actions: read
  contents: read

jobs:
  diagnose:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.user.login == 'athipan1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Print concise failed target logs
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          TARGET_RUN_ID: "30150834103"
        shell: bash
        run: |
          set -euo pipefail
          gh run view "$TARGET_RUN_ID" --repo "$REPO" --log-failed > failed.log
          echo "=== ERROR MATCHES ==="
          grep -Ei "RuntimeSafetyError|not ready|connection|refused|unauthorized|forbidden|error|failed" failed.log | tail -n 30 || true
          echo "=== FINAL 35 LINES ==="
          tail -n 35 failed.log
