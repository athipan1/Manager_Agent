services:
  database-agent:
    environment:
      # Restore canonical strategy ownership for confirmed Alpaca Paper holdings
      # before broker reconciliation evaluates semantic sync safety.
      STRATEGY_BUCKET_ASSIGNMENTS_JSON: ${STRATEGY_BUCKET_ASSIGNMENTS_JSON:-ACGL=value_rebound,ADBE=value_rebound,BKNG=value_rebound,CINF=value_rebound}
